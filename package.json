{
  "name": "typeahead-examples",
  "version": "0.4.0",
  "private": true,
  "homepage": "http://typeahead.tomocchino.com",
  "scripts": {
    "test": "jest --watch",
    "dev": "next dev",
    "lint": "next lint",
    "build": "next build",
    "start": "next start",
    "predist": "mkdir -p build package && cp -R {release/*,src} package",
    "dist": "cd package && tsc -p ../ && cp -R src/css dist && npm pack --pack-destination ../build",
    "postdist": "rm -rf package"
  },
  "dependencies": {
    "emojibase": "^6.1.0",
    "next": "^12.2.5",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/jest": "^28.1.7",
    "@types/react": "^18.0.17",
    "@types/react-dom": "^18.0.6",
    "babel-jest": "^28.1.3",
    "eslint": "^8.22.0",
    "eslint-config-next": "^12.2.5",
    "eslint-config-prettier": "^8.5.0",
    "jest": "^28.1.3",
    "typescript": "^4.7.4"
  },
  "babel": {
    "presets": [
      "next/babel"
    ]
  }
}
